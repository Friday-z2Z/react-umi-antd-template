/**
 * 使用
 * 引用 import { formatMessage } from 'umi/locale';
 * 到处语言 formatMessage({ id: 'sys.title' })
 * 
 * 切换
 * 引入 import { setLocale, getLocale } from 'umi/locale';
 * getLocale
 * 从配置中 config.js || .umirc.js 中读取local
 * setLocale
 * 设置配置 local 值
 */
export default {
  'sys.title': '标题',
};
